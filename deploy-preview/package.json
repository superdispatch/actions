{
  "name": "@actions/deploy-preview",
  "version": "1.0.3",
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/index.ts -s -o dist",
    "version": "yarn build && git add ."
  },
  "engines": {
    "node": ">=12"
  }
}
