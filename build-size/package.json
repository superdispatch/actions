{
  "name": "@actions/build-size",
  "version": "1.0.2",
  "license": "MIT",
  "scripts": {
    "build:cache": "ncc build cache/index.ts -s -o cache/dist",
    "build:report": "ncc build report/index.ts -s -o report/dist",
    "version": "yarn build:cache && yarn build:report"
  },
  "engines": {
    "node": ">=12"
  }
}
